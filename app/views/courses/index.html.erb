<h1>Courses</h1>

<table class = "table">
<tbody>
	<% @courses.each do |course| %>
	<tr>
		<td><%= course.name %> </td>
		<td class= "link"><%= link_to 'Show', course_path(course) %></td>
	</tr>
	<% end %>
</tbody>
</table>

</br>

<%= link_to 'New course', new_course_path %>